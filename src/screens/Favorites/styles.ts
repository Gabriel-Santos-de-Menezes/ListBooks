import styled from 'styled-components/native';
import { GlobalContainer, GlobalBooks } from '../../theme/globalContainer';

export const Container = styled(GlobalContainer)`

`;

export const Books = styled(GlobalBooks)`
    
`;

export const Title = styled.Text`
    font-size: 16px;
    font-weight: bold;
    margin-top: 25px;
`; 
